# Database Configuration
# In a Docker Compose environment, 'db' refers to the service name of the MongoDB container.
MONGO_URI=mongodb://db:27017/devblog

# Server Configuration
PORT=3000
NODE_ENV=production

